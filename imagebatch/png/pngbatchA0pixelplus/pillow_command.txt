from PIL import Image
im = Image.open("C:/test.png")
im.load() #для редактирования
im.save()
im.show() #открывает в фотофильтре видимо по умолчанию прозвон
print(im.format, im.size, im.mode) #->PNG (3072, 2304) RGB
im.getbands() #('R', 'G', 'B') названия привязок к параметрам , чтоли
im.info #{'dpi': (72, 72)}
px=im.load()
print (px[4,4])
px[4,4] = (0,0,0)
Image.getdata(band=None) #3=alpha
Image.putdata(data, scale=1.0, offset=0.0)

8-54